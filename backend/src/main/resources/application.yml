spring:
  application:
    name: volunteer-platform
  datasource:
    url: jdbc:mysql://localhost:3306/volunteer?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: xlyjiayoua666.
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

server:
  port: 8080

logging:
  level:
    root: INFO
    com.example.volunteer: DEBUG

app:
  jwt:
    # Base64 编码的 32 字节密钥 (256 bits)
    secret: dm9sdW50ZWVyLXBsYXRmb3JtLWp3dC1zZWNyZXQta2V5LTIwMjQ=
    expire-ms: 86400000
  storage:
    root: uploads
  terminal:
    offline-seconds: 300
