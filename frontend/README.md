# Volunteer Media Platform Frontend

## 开发
```bash
npm install
npm run dev
```
默认代理 `/api` 到 `http://localhost:8080`。

## 目录
- `src/views/Login.vue` 登录页
- `src/views/Dashboard.vue` 仪表盘占位
- 后续补充资源管理、播放设置、终端管理等页面
